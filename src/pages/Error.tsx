import React from 'react'
import { Typography } from '@mui/material'

const Error = () => {
  return (
    <Typography variant="h6" mt={20} textAlign={"center"}>
      Some Went Wrong!
    </Typography>
  );
};

export default Error;